// `#octarq-plugins` is a virtual module generated at build time from the plugin
// manifest (see web/plugins-manifest.ts). It's imported only for side effects
// (it registers the edition's UI plugins), so an empty ambient declaration is
// all TypeScript needs to accept `import "#octarq-plugins"`.
declare module "#octarq-plugins";
