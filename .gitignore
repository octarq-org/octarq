# binaries
/octarq
/dist
*.exe

# database & secrets
*.db
*.db-*
.env

# go
/vendor

# node
/node_modules
web/node_modules
web/.vite
packages/*/node_modules
packages/*/dist
examples/*/web/node_modules

# geoip
*.mmdb

# manifest-plugin install snapshots (web/scripts/install-manifest-plugins.mjs)
*.manifest-bak

# air (hot-reload tmp)
.air/

# os & IDE
.DS_Store
.claude/

# agent scratch: task briefings and handoff notes, not project docs
.agents/
.agy-specs/

# octarq-build generated frontend manifest (transient)
.octarq-frontend-plugins.json
.worktrees/

# playwright e2e artifacts (the throwaway sqlite DB itself lives in the OS temp
# dir, created and deleted by playwright.config.ts / global-teardown.ts)
playwright-report/
test-results/
