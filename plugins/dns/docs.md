# DNS & Network

The DNS plugin allows you to manage DNS records for custom domains directly within octarq.

## Features

- **Domain Management**: Add and verify domains using standard DNS challenges.
- **Provider Integrations**: Link your Cloudflare account to automatically provision records.
- **Dynamic DNS**: Issue tokens for devices to automatically update their IP address.
